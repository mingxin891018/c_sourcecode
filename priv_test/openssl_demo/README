版本介绍：
	OpenSSL官方博客，发布消息宣布其新的版本OpenSSL 1.1.1,这是OpenSSL既上个LTS（长期支持版本）1.0.2之后有一个LTS版本，承诺至少支持五年。
编译方法：
	./config -fPIC --shared --prefix=../openssl-lib --openssldir=../openssl-lib/ssl
	make
	make install

运行环境：
	export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$(pwd)/openssl-lib/lib

